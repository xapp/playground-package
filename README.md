# X.app (Playground) Package

This package will be cloned and built as a dependency if you build the `PlaygroundPackage` example:

    git clone https://github.com/xapp/playground-package.git
    cd playground-package
    swift run playgrounds <count>

For more information, visit [Swift's package manager documentation](https://www.swift.org/package-manager/).
